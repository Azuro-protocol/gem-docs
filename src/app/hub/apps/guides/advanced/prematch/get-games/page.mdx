import { Callout } from 'components'

# Get Games

<Callout type="advice">
  Before you begin working with GraphQL queries, please read the [Highlights](/subgraph/highlights) and [Request Optimizations](/subgraph/request-optimizations) articles.
</Callout>

## Fetch games

To fetch data you should use [Azuro REST API](/hub/apps/APIs/backend). The following query represents main fields required
to present a game info and its markets with outcomes.

[Data-feed API Swagger](https://api.onchainfeed.org/api/v1/public/gateway/docs#/Marker%20Manager)

<Callout type="info">
  Values of `startsAt` fields are calculated in seconds
</Callout>

<Callout type="info">
  Please pay attention, that Azuro protocol can release a new sport (e.g. esports, cricket, etc).
  We don't recommend to build navigation by raw usage of `sports` query from the subgraph.
  It's better to keep filter by sports which are supported in your app.
</Callout>
