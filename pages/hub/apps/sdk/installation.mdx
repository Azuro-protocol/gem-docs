import { Callout, Image } from 'components'

# Installation

<Callout type="info">
There are two ways to use AzuroSDK:

1. Use our sample dApp as a starting point and customize it to your needs.
2. Integrate AzuroSDK directly into your own project from scratch.

Below are setup guides for both approaches.
</Callout>

## 1. Build on top of our sample dApp

1. **Clone the SDK repository**:
```bash copy
  git clone https://github.com/Azuro-protocol/example-app
```
2. **Install Dependencies**:
```bash copy
  npm install
```
3. **Run the development server**:
```bash copy
  npm run dev
```
4. **Open the appn**:  
   Visit http://localhost:3000 in your browser.

{/* <Image
  src="/images/sdk/setupSDK.gif"
  width={1152}
  height={648}
/> */}

## 2. Use SDK in your own project

1. **Install the SDK**:
```bash copy
  npm install @azuro-org/sdk
```
2. **Install required peer dependencies**:

```bash copy
npm install @azuro-org/dictionaries@^3.0.26 graphql@^16.11.0 viem@^2.29.0 wagmi@^2.14.16
```
